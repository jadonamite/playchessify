export interface ChessProfile { address: string; username: string; displayName: string; bio: string; og: boolean; createdAt: number; updatedAt: number; usernameChangedAt: number; } export interface ProfileCheckResult { available: boolean; reason?: string; } export interface BatchProfileResult { profiles: Record<string, ChessProfile | null>; } function validateChessProfile(profile: ChessProfile): boolean { if (!profile.address || !profile.username || !profile.displayName || !profile.bio || typeof profile.og !== 'boolean' || typeof profile.createdAt !== 'number' || typeof profile.updatedAt !== 'number' || typeof profile.usernameChangedAt !== 'number') { return false; } if (profile.username.length < 1 || profile.displayName.length > 30 || profile.bio.length > 120) { return false; } return true; }