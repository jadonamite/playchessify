export interface ChessProfile { address: string; username: string; displayName: string; bio: string; og: boolean; createdAt: number; updatedAt: number; usernameChangedAt: number; } export interface ProfileCheckResult { available: boolean; reason?: string; } export interface BatchProfileResult { profiles: Record<string, ChessProfile | null>; } function validateProfile(profile: ChessProfile): boolean { const { username, displayName, bio } = profile; if (username.length === 0) return false; if (displayName.length > 30) return false; if (bio.length > 120) return false; return true; }