export interface ChessProfile { address: string; username: string; displayName: string; bio: string; og: boolean; createdAt: number; updatedAt: number; usernameChangedAt: number; } export interface ProfileCheckResult { available: boolean; reason?: string; } export interface BatchProfileResult { profiles: Record<string, ChessProfile | null>; } function validateChessProfile(profile: ChessProfile): boolean { return (typeof profile.address === 'string' && typeof profile.username === 'string' && typeof profile.displayName === 'string' && typeof profile.bio === 'string' && typeof profile.og === 'boolean' && typeof profile.createdAt === 'number' && typeof profile.updatedAt === 'number' && typeof profile.usernameChangedAt === 'number' && profile.displayName.length <= 30 && profile.bio.length <= 120); }