export interface ChessProfile { address: string; username: string; displayName: string; bio: string; og: boolean; createdAt: number; updatedAt: number; usernameChangedAt: number; } export interface ProfileCheckResult { available: boolean; reason?: string; } export interface BatchProfileResult { profiles: Record<string, ChessProfile | null>; } function validateProfileData(profile: ChessProfile): boolean { return (profile.username.length > 0 && profile.username.length <= 30) && (profile.displayName.length > 0 && profile.displayName.length <= 30) && (profile.bio.length > 0 && profile.bio.length <= 120); }