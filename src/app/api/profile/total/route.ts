import { NextResponse } from 'next/server'; import { Redis } from '@upstash/redis'; export async function GET() {   const redis = new Redis({     url: process.env.UPSTASH_REDIS_REST_URL!,     token: process.env.UPSTASH_REDIS_REST_TOKEN!,   });   try {     const total = await redis.get<number>('chess:profile:total');     return NextResponse.json({ total: total ?? 0 });   } catch (error) {     console.error('Error fetching total:', error);     return NextResponse.json({ total: 0 }, { status: 500 });   } }