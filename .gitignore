# ── Dependencies ─────────────────────────────────────────────────────────────
node_modules/
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# ── Next.js & Build Outputs ──────────────────────────────────────────────────
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts
.vercel/

# ── Environment & Secrets ────────────────────────────────────────────────────
# Ignore all env files by default, but always track .env.example
.env*
!.env.example
*.pem
*.key

# ── Testing & Coverage ───────────────────────────────────────────────────────
coverage/
*.info

# ── Logs & Debugging ─────────────────────────────────────────────────────────
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ── OS & Editor Artifacts ───────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
*.swp
*.swo
.claude/

# ── Scratch Dumps & Local Tooling ───────────────────────────────────────────
scratch/
public/scratch/
history.txt
costs-reports.json
.distribution-state.json
.topup-state.json
*handover-agents-at-work.md
*.crdownload

# ── Foundry / Smart Contracts ───────────────────────────────────────────────
celo-contracts/lib/
celo-contracts/out/
celo-contracts/cache/
celo-contracts/broadcast/
foundry.lock
.env*.local

# local key/debug helper scripts and scratch dumps (never commit)
/check-profiles.*
/claim-profiles.*
/celo-contracts/Live on Celo mainnet.txt

# auto-checkpoint committer script (DEUS mode tooling)
/.committer.sh

# local screenshot helper (never commit)
/pc-shot.mjs

# hackathon session handover (local only)
/handover-agents-at-work.md

# mentorship prep (local only)
/mentorship/

